/** Tag snapshot matching the web menus creator metrics bar (integers). */
export interface MenuTagMetrics {
  fats: number;
  protein: number;
  carbs: number;
  kcal: number;
  /** Recipe/meal card count. */
  foods: number;
  /** Standalone ingredient card count. */
  ingredients: number;
}
